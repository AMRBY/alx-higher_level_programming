#!/usr/bin/python3
""" __main__ module

"""


def is_kind_of_class(obj, a_class):
    """print_sorted is a function, it sort a list"""
    if isinstance(obj, a_class)
        return True
    else:
        return False
