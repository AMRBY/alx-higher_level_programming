#!/usr/bin/python3
def uppercase(str):
    for i in range(0, len(str):
        x = str[i]
        if x > 96 and x < 123
            up[i] = chr(x - 32)
        else:
            up[i] = str[i]
    return up
